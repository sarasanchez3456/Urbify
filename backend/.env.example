# ── Base de datos MySQL (obtienes estos valores de Aiven/Railway) ──────────────
DB_HOST=tu-host-mysql.aivencloud.com
DB_PORT=3306
DB_USER=avnadmin
DB_PASSWORD=tu_password_aqui
DB_NAME=urbify_db

# ── Autenticación ──────────────────────────────────────────────────────────────
# Genera uno fuerte: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio

# ── CORS: orígenes permitidos (separados por coma, sin espacios) ───────────────
CORS_ORIGIN=https://sarasanchez3456.github.io,http://localhost:5173

# ── Email (Resend) ─────────────────────────────────────────────────────────────
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# ── Entorno ────────────────────────────────────────────────────────────────────
NODE_ENV=production
# PORT lo pone Render automáticamente, no lo pongas tú
